<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nei10u.tip.mapper.UserMapper">
    
    <select id="getUserByRelationId" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE relation_id = #{relationId}
    </select>
    
    <select id="getUserBySpecialId" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE special_id = #{specialId}
    </select>
    
    <select id="getUserByTbUserId" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE tb_user_id = #{tbUserId}
    </select>
    
    <select id="getUserByUnionId" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE union_id = #{unionId}
    </select>
    
    <select id="getUserByPhone" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE phone = #{phone}
    </select>
    
    <select id="getUserByToken" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE token = #{token} LIMIT 1
    </select>

    <select id="getUserByMnOpenId" resultType="com.nei10u.tip.model.User">
        SELECT * FROM users WHERE mn_open_id = #{mnOpenId} LIMIT 1
    </select>
</mapper>
