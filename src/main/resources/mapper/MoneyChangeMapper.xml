<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nei10u.tip.mapper.MoneyChangeMapper">

    <insert id="insertIgnore">
        INSERT INTO money_change (user_id, order_sn, change_type, amount, uuid, create_time)
        VALUES (#{mc.userId}, #{mc.orderSn}, #{mc.changeType}, #{mc.amount}, #{mc.uuid}, #{mc.createTime})
        ON CONFLICT (uuid) DO NOTHING
    </insert>

</mapper>


